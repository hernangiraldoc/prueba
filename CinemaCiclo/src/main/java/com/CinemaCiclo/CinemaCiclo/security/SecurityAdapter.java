/*
 * Click nbfs://nbhost/SystemFileSystem/Templates/Licenses/license-default.txt to change this license
 * Click nbfs://nbhost/SystemFileSystem/Templates/Classes/Class.java to edit this template
 */
package com.CinemaCiclo.CinemaCiclo.security;

import org.springframework.http.HttpStatus;
import org.springframework.web.bind.annotation.RestController;

/*
@RestController
public class SecurityAdapter extends WebSecurityConfigurerAdapter{
    @Override
    protected void configure(HttpSecutiry http) throws Exception{
        http.authorizeRequest(a->a.antMatchers
        (...antPatterns:"/","/error","/webjars/**","/api/**").permitAll().anyRequest().authenticated())
        .exceptionHangling(e->e.authenticationEntryPoint(new HttpStatusEntryPoint(HttpStatus))
        .oauth2Login().defaultSuccesUrl(defaultSuccessUrl:"/",alwaysUse:true))
        
        http.cors().and().csrf().disable();

    }
*/
